import React from 'react';
import { Box } from '@mui/material';
import AddCustomerForm from 'components/AddCustomerForm';  // Assuming this is correct path



const Signup = () => {
  return (
    <Box>
      <AddCustomerForm />
    </Box>
  );
};

export default Signup;

//to do list
//sign up button routing - make sign up button go to signup page
//log in success routing - make login redirect to dashboard and not "/"
//log out button if I have time - do this for now
//okokokokokokokok